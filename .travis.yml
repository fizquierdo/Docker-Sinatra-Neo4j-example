sudo: required

language: ruby

services: 
  - docker

before_install:
 #- docker-compose pull
 - docker-compose build
 - docker-compose up -d
 - sleep 30
 - docker-compose ps
 - cd app && bundle install 

script:
 - sleep 30
 - rspec -f d spec

after_success:
  - docker-compose stop

after_failure:
  - docker-compose stop
  - docker-compose logs
